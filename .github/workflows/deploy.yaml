name: Deploy site to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache-bust assets and Pages fallbacks
        run: |
          V="${GITHUB_SHA:-$(date +%s)}"
          for f in site/index.html site/conferences.html site/olymp-1-4.html site/olymp-5-8.html site/olymp-samara.html; do
            sed -i.bak "s/[?]v=[^\"]*/?v=${V}/g" "$f"
            rm -f "${f}.bak"
          done
          cp site/index.html site/404.html
          touch site/.nojekyll

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
